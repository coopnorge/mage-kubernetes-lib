---
app:
  environmentVariables:
    DUPLICATE_KEY: "from-environmentVariables"

  secrets:
    example-project:
      provider: gcp
      secrets:
        NOT_DUPLICATE_KEY: duplicate-secret-value
      mountAsEnvironment: true
